// import { createFeature, createFeatureSelector, createSelector, State } from "@ngrx/store";
// import { cartState } from "../interfaces/interface.cartState";
// const selectCartState = createFeatureSelector<cartState>('cartItems');

// export const selectCartItems = createSelector(
//   selectCartState,
//   (state) => {return state.cartItems}
// );
// export const cartselect=createSelector((state: {cart:Book[] })=>state.cart)
// export const cartselect=createSelector((state:{cart:Book[] })=>state.cart,(cart)=>cart)


// export const cartselect = createSelector(
//     (state: AppState) => state.cart,
//     (cart) => cart // Return the 'cart' property
//   );
// const getcartstate=(state=>state.)
// export const getcart=createSelector(
//     (state)=>{return state}
//     )

// export const cartselect=createSelector((state)=>state)
